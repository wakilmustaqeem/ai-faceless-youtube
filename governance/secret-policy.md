# Secret Policy

- Real API keys, OAuth tokens, refresh tokens and channel credentials never enter Git history.
- Runtime secrets must be supplied through the execution environment or GitHub Actions Secrets.
- This repository has no dependency on HidayatTube credentials.
- Provider activation requires an independent connectivity test.
